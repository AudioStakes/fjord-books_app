<h1><%= t('views.common.title_edit', name: Comment.model_name.human) %></h1>

<h2><%= t(@comment.commentable.class.model_name.human) %>: <%= @comment.commentable.title %></h2>

<%= render 'comments/form', comment: @comment %>

<%= link_to t('views.common.back'), @comment.commentable %>
